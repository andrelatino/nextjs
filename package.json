{
  "name": "nextjs-blog",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "deploy": "rimraf node_modules/.cache && next build && touch out/.nojekyll && gh-pages -d out"
  },
  "dependencies": {
    "date-fns": "^2.30.0",
    "next": "^13.4.19",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "rimraf": "^5.0.10"
  }
}
